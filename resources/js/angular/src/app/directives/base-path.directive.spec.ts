import { BasePathDirective } from './base-path.directive';

describe('ImagePathDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagePathDirective();
    expect(directive).toBeTruthy();
  });
});
